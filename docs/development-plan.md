# CryptoTrading 개발 계획

## 현재 상태 (2026-03-30 기준)

### 완료
- UI 레이아웃 완성 (HTML/CSS 전체)
- `js/main.js` — 탭 전환, 모달, 설정 드롭다운
- `js/order.js` — 선물 매매, 레버리지, TP/SL UI, 슬라이더
- `js/api.js` — Binance WebSocket 연결 관리 (자동 재연결, 심볼/타임프레임 전환)
- `js/ticker.js` — 티커 바 + 사이드바 실시간 가격/변동률 업데이트
- `js/orderbook.js` — 호가창 실시간 렌더링 (depth 바, 누적 수량)
- `js/trades.js` — 체결 내역 실시간 업데이트
- `js/chart.js` — Lightweight Charts, REST API 캔들, WebSocket 실시간 캔들, 타임프레임/차트유형 전환

### 미완료
- 없음 (전체 완료)

---

## 거래 모드

| 모드 | 설명 |
|------|------|
| 선물 (Futures) | USDT-M 무기한 계약, 레버리지 1x~125x, 롱/숏 |

### 잔고
- 초기 잔고: **100 USDT**
- `localStorage`에 저장 (새로고침 후 유지)

### 포지션 관리 규칙

- 포지션 방향: **헤지 모드** — 동일 코인에서 롱/숏 동시 보유 가능
- 동일 코인 + 동일 방향 + 동일 레버리지: 평균 단가 합산
- 동일 코인 + 동일 방향 + 다른 레버리지: 별개 포지션
- 동일 코인 + 다른 방향 (롱/숏): 항상 별개 포지션 (반대 방향 진입 시 기존 포지션 청산 없이 새 포지션 생성)

---

## 거래 마켓

### USDT 마켓 (2개 고정)

| # | 심볼 | 이름 |
|---|------|------|
| 1 | BTCUSDT | Bitcoin |
| 2 | ETHUSDT | Ethereum |

> 고정 목록 방식 채택 — 실시간 순위 변동 반영 없음. 목록 변경 시 `js/api.js` 상수만 수정.

---

## 선물 거래 사양

| 항목 | 내용 |
|------|------|
| 계약 종류 | USDT-M 무기한 계약 (Perpetual) |
| 레버리지 | 1x ~ 125x |
| 포지션 방향 | 롱 (Long) / 숏 (Short) |
| 펀딩비 | 8시간마다 부과 (바이낸스 실제 펀딩비율 적용) |
| 청산 | 유지 증거금 미달 시 강제 청산 시뮬레이션 |

### 주요 계산식
- **진입 증거금** = 포지션 크기 ÷ 레버리지
- **청산가 (롱)** = 진입가 × (1 − 1/레버리지)
- **청산가 (숏)** = 진입가 × (1 + 1/레버리지)
- **미실현 PnL (롱)** = (현재가 − 진입가) × 수량
- **미실현 PnL (숏)** = (진입가 − 현재가) × 수량
- **ROI** = 미실현 PnL ÷ 진입 증거금 × 100%
- **펀딩비** = 포지션 크기 × 펀딩비율 (양수: 롱→숏 지급, 음수: 숏→롱 지급)

---

## 포트폴리오 페이지

- 방식: **별도 페이지** (`portfolio.html`)
- 표시 내용:
  - 총 자산 (선물 잔고 + 선물 미실현 PnL)
  - 선물 보유 포지션별 코인, 방향(롱/숏), 레버리지, 진입가, 현재가, 미실현 PnL, ROI
  - 수익률 그래프:
    - 라인 차트: 시간대별 총 자산 변화
    - 바 차트: 일별 손익

---

## UI 레이아웃

### 헤더
- 탭: **선물**, **포트폴리오**, **거래내역**
- 거래내역 탭 우측에 **설정 버튼 (⚙)** 배치
  - 클릭 시 드롭다운 메뉴:
    - 전체 초기화 (클릭 시 확인 팝업 후 실행)

### 사이드바
- 탭: **USDT**

### TP/SL 토글 섹션
- ROI(%) / PnL(USDT) 방식 선택 탭
- TP 가격 입력란
- SL 가격 입력란

### 트레이드 폼

- 레버리지 선택 (1x ~ 125x)
- 롱 / 숏 버튼
- 지정가 / 시장가 탭
- 가격, 수량 입력
- 슬라이더 (25% / 50% / 75% / 100%)
- TP/SL 토글 섹션 (접었다 펼치기)

### 하단 패널 (바이낸스 방식)
탭 구성:

| 탭 | 내용 |
|----|------|
| 포지션 | 현재 보유 포지션 |
| 미체결 주문 | 대기 중인 지정가 주문 (조회/취소) |
| 체결 내역 | 최근 체결 내역 |
| 자산 | 선물 잔고 현황 |

**포지션 탭 표시 항목:**

| 항목 | 비고 |
|------|------|
| 코인 / 방향 | 롱/숏 색상 구분 |
| 레버리지 | |
| 진입가 | 평균 진입가 |
| 현재가 | 실시간 |
| 수량 | 보유 수량 |
| 미실현 PnL | 실시간 |
| ROI | 수익률 % |
| 청산가 | |
| TP 가격 | 설정 시 표시 |
| SL 가격 | 설정 시 표시 |
| 청산 버튼 | |
| 투자금액 | |

---

## 페이지 구성

- 헤더 탭: **선물**, **포트폴리오**, **거래내역**

---

## 거래내역 페이지

- 방식: **별도 페이지** (`history.html`)
- 탭 구분으로 항목별 조회

**체결 내역 탭**
| 컬럼 |
|------|
| 시각 |
| 코인 |
| 방향 (매수/매도) |
| 주문 유형 (지정가/시장가) |
| 체결가 |
| 수량 |
| 총금액 |
| 수수료 |

**미체결 주문 탭**
| 컬럼 |
|------|
| 주문 시각 |
| 코인 |
| 방향 (매수/매도) |
| 주문 유형 (지정가/시장가) |
| 주문가 |
| 수량 |
| 총금액 |
| 수정 버튼 |
| 취소 버튼 |

**TP/SL 체결 내역 탭**
| 컬럼 |
|------|
| 체결 시각 |
| 코인 |
| 구분 (TP / SL) |
| 트리거 방식 (ROI / PnL) |
| 트리거 가격 |
| 체결가 |
| 청산 수량 |
| 실현 PnL |
| 수수료 |

**펀딩비 내역 탭**
| 컬럼 |
|------|
| 시각 (8시간마다) |
| 코인 |
| 포지션 방향 (롱/숏) |
| 포지션 크기 |
| 펀딩비율 |
| 부과 금액 (USDT) |
| 수취/지급 구분 |

---

## 리셋 기능

- 전체 초기화 버튼 제공 (실수 방지를 위해 확인 팝업 후 실행)
- 초기화 항목:
  - 선물 잔고 → 100 USDT
  - 모든 포지션
  - 미체결 주문 전체
  - 거래내역 전체
  - TP/SL 설정 전체
- `localStorage` 전체 삭제 후 초기값으로 재설정

---

## 알림

- 방식: **화면 내 토스트 팝업** (우측 상단)
- 알림 발생 이벤트:
  - 지정가 주문 체결
  - TP 도달 → 자동 체결
  - SL 도달 → 자동 체결
  - 선물 강제 청산 발생
  - 펀딩비 부과 (8시간마다)
- 자동으로 사라지는 방식 (몇 초 후 dismiss)

---

## 미체결 주문 관리

- **조회**: 미체결 주문 목록 표시 (코인, 방향, 가격, 수량, 주문 시각)
- **취소**: 개별 취소 지원, 취소 시 예약 증거금 반환
- 수정/취소 시 수수료 없음 (체결 시에만 수수료 발생)
- 미체결 주문 데이터 `localStorage` 저장 (새로고침 후 유지)

---

## 수수료 정책

| 구분 | Maker (지정가) | Taker (시장가) |
|------|--------------|--------------|
| 선물 | 0.1% | 0.1% |

- 매수/매도 각각 1회씩 부과
- 받는 USDT(수익)에서 차감

---

## 기술 스택

| 역할 | 도구 | 선택 이유 |
|------|------|-----------|
| 시세 데이터 | Binance Public REST API | 무료, 인증 불필요 |
| 실시간 데이터 | Binance WebSocket Stream | 무료, 실시간 가격/호가/체결 |
| 차트 | TradingView Lightweight Charts | 무료 오픈소스, 캔들차트 지원 |
| 배포 | Netlify (무료 플랜) | 정적 사이트 배포 |
| 백엔드 | 없음 (Pure Frontend) | 공개 API만 사용 |

---

## 사용 API

### REST API (`api.binance.com`)
- `GET /api/v3/ticker/24hr` — 24시간 시세 (가격, 변동률, 거래량)
- `GET /api/v3/depth` — 호가창 스냅샷
- `GET /api/v3/trades` — 최근 체결 내역
- `GET /api/v3/klines` — 캔들 데이터

### WebSocket (`stream.binance.com`)
- `<symbol>@ticker` — 실시간 가격/24h 시세
- `<symbol>@depth20` — 실시간 호가창
- `<symbol>@trade` — 실시간 체결 내역
- `<symbol>@kline_<interval>` — 실시간 캔들

### 선물 REST API (`fapi.binance.com`)
- `GET /fapi/v1/ticker/24hr` — 선물 24시간 시세
- `GET /fapi/v1/depth` — 선물 호가창 스냅샷
- `GET /fapi/v1/trades` — 선물 체결 내역
- `GET /fapi/v1/klines` — 선물 캔들 데이터
- `GET /fapi/v1/premiumIndex` — 현재 펀딩비율 및 마크 가격
- `GET /fapi/v1/fundingRate` — 펀딩비 이력

### 선물 WebSocket (`fstream.binance.com`)
- `<symbol>@ticker` — 선물 실시간 시세
- `<symbol>@depth20` — 선물 실시간 호가창
- `<symbol>@trade` — 선물 실시간 체결 내역
- `<symbol>@kline_<interval>` — 선물 실시간 캔들
- `<symbol>@markPrice` — 마크 가격 + 펀딩비율 실시간

---

## 개발 단계

### Phase 1 — 실시간 가격 연동 ✅ 완료
**목표:** 하드코딩 데이터를 실제 Binance 데이터로 교체

- [x] `js/api.js` — Binance WebSocket 관리 (자동 재연결, 심볼/타임프레임 전환)
- [x] 티커 바 (가격, 24h 변동, 고/저가, 거래량) 실시간 업데이트
- [x] 사이드바 코인 목록 가격/변동률 실시간 업데이트
- [x] 호가창 미드 프라이스 실시간 업데이트

**산출물:** `js/api.js`, `js/ticker.js`

---

### Phase 2 — 캔들 차트 구현 ✅ 완료
**목표:** TradingView Lightweight Charts로 실제 캔들 차트 렌더링

- [x] Lightweight Charts 라이브러리 추가 (`index.html`)
- [x] `js/chart.js` — 캔들 차트 초기화 및 렌더링
- [x] REST API로 과거 캔들 데이터 로드 (Binance `/api/v3/klines`, 500봉)
- [x] WebSocket으로 실시간 캔들 업데이트
- [x] 타임프레임 버튼 (1m / 5m / 15m / 1H / 4H / 1D / 1W) 동작
- [x] 라인 차트 / 캔들 차트 전환 버튼 동작
- [x] `css/chart.css` — 차트 영역 스타일 조정

**산출물:** `js/chart.js`, `css/chart.css` 수정

---

### Phase 3 — 호가창 & 체결 내역 연동 ✅ 완료
**목표:** 실시간 호가창/체결 내역 렌더링

- [x] `js/orderbook.js` — 호가창 렌더링 및 실시간 업데이트
- [x] 매도 호가 (asks) / 매수 호가 (bids) 정렬 및 누적 계산
- [x] 깊이 바 (`--depth` CSS 변수) 동적 계산
- [x] `js/trades.js` — 최근 체결 내역 실시간 업데이트
- [x] 체결 방향(매수/매도)에 따른 색상 표시

**산출물:** `js/orderbook.js`, `js/trades.js`

---

### Phase 4 — 코인 전환 기능 ✅ 완료
**목표:** 사이드바에서 코인 클릭 시 모든 패널 동기화

- [x] 코인 클릭 이벤트 처리
- [x] 활성 WebSocket 연결 해제 후 새 코인으로 재연결
- [x] 차트, 티커, 호가창, 체결내역 동시 전환
- [x] 타임프레임 유지 (코인 변경 후에도 선택한 타임프레임 유지)
- [x] 검색 필터 기능

**산출물:** `js/main.js` 업데이트

---

### Phase 5 — 모의 거래 기능 ✅ 완료
**목표:** 실제 체결 없는 시뮬레이션 거래

- [x] 매수/매도 주문 처리 로직 (`js/order.js`)
- [x] 지정가 / 시장가 주문 탭 전환
- [x] 슬라이더로 비율 입력 (25% / 50% / 75% / 100%)
- [x] 가격 × 수량 = 총액 자동 계산
- [x] 보유 잔고 기반 주문 가능 여부 검증
- [x] 지정가 주문 체결 시뮬레이션 (현재가 도달 시 자동 체결)
- [x] 보유 자산 업데이트 (헤더 USDT 잔고)
- [x] 초기 잔고: 100 USDT
- [x] `localStorage` 영구 저장 (잔고, 체결 내역)
- [x] 하단 패널 체결 내역 탭 데이터 표시
- [x] 자산 탭 실시간 잔고 표시
- [x] 전체 초기화 버튼 동작

**산출물:** `js/order.js` 업데이트

---

### Phase 6 — TP/SL (Take Profit / Stop Loss) ✅ 완료
**목표:** 주문 시 익절/손절 조건 설정 및 자동 체결 시뮬레이션

- [x] ROI(%) / USDT 방식 전환
- [x] 주문 시 TP/SL 동시 등록 (포지션 오브젝트에 저장)
- [x] 실시간 가격 모니터링 → TP/SL 도달 시 자동 청산
- [x] TP/SL localStorage 영구 저장

**산출물:** `js/positions.js` (통합)

---

### Phase 7 — 선물 거래 기능 ✅ 완료
**목표:** 레버리지 기반 롱/숏 포지션 시뮬레이션

- [x] 레버리지 선택 (1x ~ 125x)
- [x] 헤지 모드 — 롱/숏 동시 보유 (동일 레버리지: 평균 합산, 다른 레버리지: 별개)
- [x] 롱/숏 지정가/시장가 주문
- [x] 지정가 등록 시 증거금 즉시 예약, 취소 시 반환
- [x] 청산가 실시간 계산 및 표시
- [x] 미실현 PnL / ROI 실시간 표시
- [x] 강제 청산 시뮬레이션 (청산가 도달 시)
- [x] 수동 청산 버튼 (포지션 탭)
- [x] 포지션 / 잔고 localStorage 저장

**산출물:** `js/order.js` + `js/positions.js` (통합)

---

## 파일 구조

```
CryptoTrading/
├── index.html
├── history.html
├── portfolio.html
├── css/
│   ├── reset.css
│   ├── layout.css
│   ├── header.css
│   ├── sidebar.css
│   ├── chart.css
│   ├── orderbook.css
│   ├── trade.css
│   ├── tpsl.css
│   ├── futures.css
│   ├── bottom-panel.css
│   ├── modal.css
│   ├── toast.css
│   ├── history.css
│   └── portfolio.css
├── js/
│   ├── main.js       # UI 인터랙션, 탭 전환, 모달
│   ├── api.js        # Binance WebSocket 연결 관리
│   ├── ticker.js     # 티커 바 업데이트
│   ├── chart.js      # 캔들 차트
│   ├── orderbook.js  # 호가창
│   ├── trades.js     # 체결 내역
│   ├── order.js      # 선물 모의 거래, 잔고 관리
│   ├── positions.js  # 포지션 렌더링, 자동 체결
│   ├── funding.js    # 펀딩비
│   ├── history.js    # 거래내역 페이지
│   ├── portfolio.js  # 포트폴리오 페이지
│   └── toast.js      # 토스트 알림
└── docs/
    └── development-plan.md
```
